<h1>Messages</h1>

<div id="messages">
  <% @messages.each do |message| %>
    <%= render message %>
  <% end %>
</div>

<%= link_to "New message", new_message_path %>

<hr>

<%= current_user.find_votes_for_class(Message, vote_scope: "like").pluck(:id) %>
<%= current_user.find_up_votes_for_class(Message, vote_scope: "like").pluck(:id) %>
<%= current_user.find_down_votes_for_class(Message, vote_scope: "like").pluck(:id) %>
